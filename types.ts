// types.ts
export interface Store {
    id: string; // UUID
    name: string;
    userId: string; // Assuming this is a UUID or string
    createdAt: string; // ISO date string
    updatedAt: string; // ISO date string
  }
  